import React, { ReactElement } from "react";
import "./Content.css";

export interface ContentProps {}

export const Content = (props: ContentProps): ReactElement => {
  return <div className='content'>BBB</div>;
};
