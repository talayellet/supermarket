import React, { ReactElement } from "react";
import "./Sidebar.css";

export interface SidebarProps {}

export const Sidebar = (props: SidebarProps): ReactElement => {
  return <div className='sidebar'>AAA</div>;
};
